//create a function called notBad
//takes a single argument, a string
//find first appearance of substring "not" and "bad"
//if bad follows not, then replace it with "good", return
//if not, return original

str = "Gladio is not a bad character at all.";

str2 = "Monster Hunter World is good, but not great.";

str3 = "John Wick is a great film series.";

//these var need to be taken in and stored as var then logged

function notBad(string) {
    string.str.search("not", "bad");
}